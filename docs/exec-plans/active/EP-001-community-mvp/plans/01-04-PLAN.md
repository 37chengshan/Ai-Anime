---
phase: 01-community-mvp
plan: "01-04"
type: execute
wave: 2
depends_on: ["01-03"]
files_modified:
  - apps/api/app/routers/comments.py
  - apps/api/app/routers/posts.py
  - apps/api/app/repositories/comment.py
  - apps/api/app/services/comment.py
  - apps/api/app/schemas/comments.py
  - apps/web/src/components/domain/CommentSection.tsx
  - apps/web/src/components/domain/WorkCard.tsx
autonomous: true
requirements:
  - REQ-1.4.1
---

<objective>
实现评论与互动系统：评论列表/发表、点赞、收藏的后端 API 与前端组件集成。
</objective>

<must_haves>
  truths:
    - 用户可对作品进行评论、点赞、收藏
    - 评论按时间或热度排序
    - 用户只能删除自己的评论
  artifacts:
    - apps/web/src/components/domain/CommentSection.tsx（API 驱动）
    - apps/api/app/routers/comments.py（完整实现）
    - apps/api/app/repositories/comment.py（数据访问层）
    - apps/api/app/services/comment.py（业务逻辑层）
  key_links:
    - GET /api/v1/posts/{id}/comments
    - POST /api/v1/posts/{id}/comments
    - DELETE /api/v1/comments/{id}
    - POST /api/v1/posts/{id}/like
    - DELETE /api/v1/posts/{id}/like
    - POST /api/v1/posts/{id}/favorite
    - DELETE /api/v1/posts/{id}/favorite
---

<tasks>

<task id="01-04-W2-01">
<name>Create comment repository (apps/api/app/repositories/comment.py)</name>
<read_first>
- apps/api/app/repositories/user.py
- apps/api/app/models/content.py
</read_first>
<action>
1. Create file apps/api/app/repositories/comment.py with async SQLAlchemy 2.0 repository class CommentRepository.
2. Implement __init__(self, session: AsyncSession) storing self._session.
3. Implement async def get_by_id(self, comment_id: UUID) -> Optional[Comment]: query Comment by id, return scalar_one_or_none().
4. Implement async def list_by_post(self, post_id: UUID, page: int, limit: int, sort: str) -> Tuple[List[Comment], int]: build query filtered by post_id and status="visible", apply sort (newest=created_at.desc, oldest=created_at.asc, popular=like_count.desc), apply pagination with offset=(page-1)*limit, get total count separately. Return (comments, total).
5. Implement async def create(self, comment_data: dict) -> Comment: create Comment instance, session.add, flush, refresh, return.
6. Implement async def delete(self, comment_id: UUID) -> bool: query Comment, session.delete, flush, return rowcount > 0.
7. Implement async def get_like_count(self, comment_id: UUID) -> int: query CommentLike with given comment_id, count.
8. Implement async def add_like(self, comment_id: UUID, user_id: UUID) -> CommentLike: create CommentLike, session.add, flush, return.
9. Implement async def remove_like(self, comment_id: UUID, user_id: UUID) -> bool: query CommentLike by comment_id and user_id, delete, flush, return rowcount > 0.
10. Implement async def is_liked(self, comment_id: UUID, user_id: UUID) -> bool: query CommentLike by comment_id and user_id, return scalar_one_or_none() is not None.
11. Add typing imports: Tuple from typing, UUID from uuid, select/from sqlalchemy, AsyncSession from sqlalchemy.ext.asyncio, Comment/PostLike/PostFavorite from app.models.content, CommentLike from new model (create if not exists in models/content.py).
</action>
<acceptance_criteria>
- grep -n "class CommentRepository" apps/api/app/repositories/comment.py
- grep -n "async def list_by_post" apps/api/app/repositories/comment.py
- grep -n "async def create" apps/api/app/repositories/comment.py
- grep -n "async def delete" apps/api/app/repositories/comment.py
- grep -n "async def add_like" apps/api/app/repositories/comment.py
- grep -n "async def remove_like" apps/api/app/repositories/comment.py
</acceptance_criteria>
</task>

<task id="01-04-W2-02">
<name>Add CommentLike model and create comment service</name>
<read_first>
- apps/api/app/models/content.py
- apps/api/app/services/user.py
</read_first>
<action>
1. Add to apps/api/app/models/content.py after Comment class:
   class CommentLike(Base):
       __tablename__ = "comment_likes"
       id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
       comment_id = Column(UUID(as_uuid=True), ForeignKey("comments.id"), nullable=False)
       user_id = Column(UUID(as_uuid=True), ForeignKey("users.id"), nullable=False)
       created_at = Column(DateTime(timezone=True), server_default=func.now())
       __table_args__ = ({"unique_constraint": ("comment_id", "user_id")},)
2. Create apps/api/app/services/comment.py with class CommentService.
3. In __init__, accept session: AsyncSession, set self._repo = CommentRepository(session).
4. Implement async def get_comments(self, post_id: UUID, page: int, limit: int, sort: str, current_user_id: Optional[UUID]) -> Tuple[List[dict], int]: call repo.list_by_post, for each comment build author info from UserRepository, add is_liked field, return (comment_dicts, total).
5. Implement async def create_comment(self, post_id: UUID, author_user_id: UUID, content: str, parent_comment_id: Optional[UUID], ai_assisted: bool) -> Comment: call repo.create, also increment Post.comment_count by 1. Return Comment.
6. Implement async def delete_comment(self, comment_id: UUID, user_id: UUID) -> bool: verify comment exists and author_user_id matches user_id, call repo.delete, decrement Post.comment_count. Return True on success.
7. Implement async def like_comment(self, comment_id: UUID, user_id: UUID) -> CommentLike: call repo.add_like, increment Comment.like_count. Return CommentLike.
8. Implement async def unlike_comment(self, comment_id: UUID, user_id: UUID) -> bool: call repo.remove_like, decrement Comment.like_count. Return True on success.
9. Import AsyncSession, UUID, Optional, Tuple from typing; CommentRepository; Post and Comment models.
</action>
<acceptance_criteria>
- grep -n "class CommentLike" apps/api/app/models/content.py
- grep -n "class CommentService" apps/api/app/services/comment.py
- grep -n "async def get_comments" apps/api/app/services/comment.py
- grep -n "async def create_comment" apps/api/app/services/comment.py
- grep -n "async def delete_comment" apps/api/app/services/comment.py
</acceptance_criteria>
</task>

<task id="01-04-W2-03">
<name>Implement comment router endpoints</name>
<read_first>
- apps/api/app/routers/comments.py
- apps/api/app/schemas/comments.py
- apps/api/app/utils/auth.py
</read_first>
<action>
1. In apps/api/app/routers/comments.py, add imports: Depends from fastapi, get_current_user_id from app.utils.auth, CommentService from app.services.comment, UserRepository from app.repositories.user, AsyncSession from app.core.db, get_db from app.core.db (or import the dependency).
2. Update list_comments: inject current_user_id: Optional[str] = Depends(get_optional_user_id), convert to UUID if not None. Instantiate CommentService(session), call await service.get_comments(post_id, page, limit, sort, current_user_id). Build PaginatedResponse with CommentResponse items. Return it.
3. Update create_comment: inject current_user_id: str = Depends(get_current_user_id). Instantiate UserRepository to resolve clerk_user_id -> internal UUID (get_by_clerk_id). Instantiate CommentService. Call await service.create_comment(post_id, user_uuid, comment.content, comment.parent_comment_id, comment.ai_assisted). Return CommentResponse.
4. Update delete_comment: inject current_user_id: str = Depends(get_current_user_id). Resolve clerk_user_id to UUID. Call await service.delete_comment(comment_id, user_uuid). On HTTPException 404 if comment not found or user mismatch.
5. Add to CommentResponse in schemas/comments.py: add is_liked: bool = Field(default=False) and is_own: bool = Field(default=False).
6. Verify get_db dependency exists in app.core.db for session injection.
</action>
<acceptance_criteria>
- grep -n "get_current_user_id" apps/api/app/routers/comments.py
- grep -n "get_optional_user_id" apps/api/app/routers/comments.py
- grep -n "CommentService" apps/api/app/routers/comments.py
- grep -n "is_liked" apps/api/app/schemas/comments.py
- grep -n "is_own" apps/api/app/schemas/comments.py
</acceptance_criteria>
</task>

<task id="01-04-W2-04">
<name>Implement like/favorite endpoints in posts router</name>
<read_first>
- apps/api/app/routers/posts.py
- apps/api/app/models/content.py
</read_first>
<action>
1. In apps/api/app/routers/posts.py, add imports: get_current_user_id from app.utils.auth, UserRepository from app.repositories.user, AsyncSession/get_db from app.core.db.
2. Update like_post: inject current_user_id: str = Depends(get_current_user_id). Resolve clerk_user_id to UUID via UserRepository. Check if PostLike already exists (query PostLike where post_id and user_id). If exists return 200. If not, create new PostLike, flush, increment Post.like_count by 1, return 200.
3. Update unlike_post: inject current_user_id. Resolve UUID. Query and delete PostLike, decrement Post.like_count. Return 204.
4. Update favorite_post: same pattern as like_post but using PostFavorite model. Increment Post.favorite_count.
5. Update unfavorite_post: same as unlike_post but using PostFavorite, decrement favorite_count.
6. For each endpoint, add proper HTTPException 404 for post not found (query Post first).
</action>
<acceptance_criteria>
- grep -n "like_post" apps/api/app/routers/posts.py
- grep -n "favorite_post" apps/api/app/routers/posts.py
- grep -n "PostLike" apps/api/app/routers/posts.py
- grep -n "PostFavorite" apps/api/app/routers/posts.py
</acceptance_criteria>
</task>

<task id="01-04-W2-05">
<name>Add like/favorite state to post schemas</name>
<read_first>
- apps/api/app/schemas/posts.py
</read_first>
<action>
1. In apps/api/app/schemas/posts.py, add to PostListResponse and PostResponse: is_liked: bool = Field(default=False) and is_favorited: bool = Field(default=False).
2. These fields will be populated in the service layer when post listing/detail endpoints are implemented. For now, add the field definitions so the schema is ready.
3. Also add like_count and favorite_count to PostResponse if not already present (PostListResponse should have them per the model).
</action>
<acceptance_criteria>
- grep -n "is_liked" apps/api/app/schemas/posts.py
- grep -n "is_favorited" apps/api/app/schemas/posts.py
</acceptance_criteria>
</task>

<task id="01-04-W2-06">
<name>Migrate CommentSection component to API-driven</name>
<read_first>
- apps/web/src/components/domain/CommentSection.tsx
- apps/api/app/schemas/comments.py
</read_first>
<action>
1. In apps/web/src/components/domain/CommentSection.tsx, replace the mock Comment interface with a type imported or defined from API response shape: id, post_id, author: { id, username, display_name, avatar_url }, content, like_count, is_liked, is_own, created_at, updated_at.
2. Replace the mock CURRENT_USER with a useUser() hook call (or Clerk's useUser) to get the authenticated user.
3. Replace the hardcoded initialComments prop usage with a useSWR or fetch call to GET /api/v1/posts/{postId}/comments on mount. Show loading state while fetching.
4. Replace handleSubmit to POST to /api/v1/posts/{postId}/comments with the comment content. On success, prepend the new comment to the local list.
5. Handle comment deletion: add delete button (visible only when is_own=true) that calls DELETE /api/v1/comments/{commentId}. On success remove from local list.
6. Handle comment likes: for each comment's like button, call POST /api/v1/comments/{commentId}/like or DELETE based on is_liked state. Toggle local like_count and is_liked optimistically.
7. Keep the existing Editorial/Magazine styling unchanged. Do not modify CSS classes or visual layout.
8. Add @typescript-eslint/no-explicit-any suppressions if needed for API response types during migration.
</action>
<acceptance_criteria>
- grep -n "useSWR\|fetch\|axios" apps/web/src/components/domain/CommentSection.tsx
- grep -n "POST.*comments" apps/web/src/components/domain/CommentSection.tsx
- grep -n "DELETE.*comments" apps/web/src/components/domain/CommentSection.tsx
- grep -n "is_own" apps/web/src/components/domain/CommentSection.tsx
</acceptance_criteria>
</task>

<task id="01-04-W2-07">
<name>Add interaction buttons to WorkCard</name>
<read_first>
- apps/web/src/components/domain/WorkCard.tsx
</read_first>
<action>
1. In apps/web/src/components/domain/WorkCard.tsx, add Heart (like) and Bookmark (favorite) icon buttons alongside existing controls.
2. Each button should show filled/outlined state based on is_liked and is_favorited props passed to WorkCard.
3. On click, call POST /api/v1/posts/{postId}/like or DELETE based on state. Optimistically update local count and filled state.
4. Display like_count and favorite_count numbers next to the icons.
5. The buttons must use the existing editorial styling (border-radius: 0, specific color tokens).
</action>
<acceptance_criteria>
- grep -n "Heart\|Bookmark" apps/web/src/components/domain/WorkCard.tsx
- grep -n "like_count\|favorite_count" apps/web/src/components/domain/WorkCard.tsx
- grep -n "is_liked\|is_favorited" apps/web/src/components/domain/WorkCard.tsx
</acceptance_criteria>
</task>

<task id="01-04-W2-08">
<name>Add comment schema for request/response validation</name>
<read_first>
- apps/api/app/schemas/comments.py
</read_first>
<action>
1. In apps/api/app/schemas/comments.py, ensure CommentResponse includes all fields returned by the API: id, post_id, author (CommentAuthorResponse), parent_comment_id, content, status, ai_assisted, like_count, is_liked, is_own, replies (List[CommentResponse]), created_at, updated_at.
2. Ensure CommentCreate includes: content, post_id (from path), parent_comment_id (optional), ai_assisted (optional default False).
3. Add CommentLikeResponse schema: id, comment_id, user_id, created_at.
4. Add PostLikeResponse and PostFavoriteResponse schemas for like/favorite toggle responses.
</action>
<acceptance_criteria>
- grep -n "is_liked" apps/api/app/schemas/comments.py
- grep -n "is_own" apps/api/app/schemas/comments.py
- grep -n "CommentLikeResponse" apps/api/app/schemas/comments.py
</acceptance_criteria>
</task>

</tasks>
