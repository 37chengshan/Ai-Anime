---
phase: 01-community-mvp
plan: "01-03"
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - apps/web/src/app/(feed)/page.tsx
  - apps/web/src/app/(feed)/following/page.tsx
  - apps/web/src/app/(feed)/trending/page.tsx
  - apps/web/src/app/(feed)/tags/[slug]/page.tsx
  - apps/web/src/app/post/[postId]/page.tsx
  - apps/web/src/app/creator/[handle]/page.tsx
  - apps/web/src/app/search/page.tsx
  - apps/api/app/routers/posts.py
  - apps/api/app/repositories/post.py
  - apps/api/app/services/post.py
autonomous: true
requirements:
  - REQ-1.3.1
  - REQ-1.3.2
  - REQ-1.3.3
  - REQ-1.3.4
  - REQ-1.3.5

must_haves:
  truths:
    - 首页瀑布流展示作品
    - 作品详情页可访问
    - 创作者主页展示作品
    - 标签页可筛选
    - 搜索页可查找
  artifacts:
    - apps/web/src/app/(feed)/page.tsx
    - apps/web/src/app/post/[postId]/page.tsx
    - apps/web/src/app/creator/[handle]/page.tsx
    - apps/api/app/routers/posts.py
  key_links:
    - GET /api/v1/posts -> 作品列表
    - GET /api/v1/posts/{post_id} -> 作品详情
    - GET /api/v1/tags -> 标签列表
    - GET /api/v1/users/{handle} -> 创作者信息
---

<objective>
实现作品展示全流程：首页瀑布流、详情页、创作者主页、标签页、搜索页、关注流、热门流
</objective>

<tasks>

<task id="1-3-W2-01">
<name>实现作品列表 API (GET /api/v1/posts)</name>
<read_first>
- apps/api/app/models/content.py
- apps/api/app/schemas/posts.py
- apps/api/app/routers/posts.py
</read_first>
<action>
1. 创建 `apps/api/app/repositories/post.py`，实现 `PostRepository` 类：
   - `find_all(filters: dict, limit: int, cursor: Optional[str]) -> Tuple[List[Post], Optional[str]]` - 支持 status=published 筛选、tag 过滤、author_id 过滤、排序(sort: latest/popular/trending)
   - `find_by_id(post_id: UUID) -> Optional[Post]` - 根据 ID 查找已发布作品
   - `find_by_slug(slug: str) -> Optional[Post]` - 根据 slug 查找

2. 在 `apps/api/app/services/post.py` 创建 `PostService` 类，封装业务逻辑

3. 在 `apps/api/app/routers/posts.py` 实现 `list_posts` 端点：
   - query 参数: page, limit, cursor, status, tag, author_id, sort
   - 返回 `PaginatedResponse[PostListResponse]`
   - 排序逻辑: latest=published_at DESC, popular=like_count DESC, trending=like_count/week DESC
   - 仅返回 status=published 的作品

4. 创建 `apps/api/app/routers/tags.py` 路由，实现 `GET /api/v1/tags` 返回标签列表

5. 创建 `apps/api/app/routers/users.py` 路由，实现 `GET /api/v1/users/{handle}` 返回创作者信息

6. 在 `apps/api/app/main.py` 注册新路由
</action>
<acceptance_criteria>
- grep -r "def list_posts" apps/api/app/ -> 找到 list_posts 函数定义
- grep -r "def find_all" apps/api/app/repositories/ -> 找到 find_all 方法
- grep -r "GET.*/api/v1/posts" apps/api/app/main.py -> 路由已注册
- curl 测试: GET /api/v1/posts?limit=2 返回 200 状态码和分页结构
</acceptance_criteria>
</task>

<task id="1-3-W2-02">
<name>实现作品详情 API (GET /api/v1/posts/{post_id})</name>
<read_first>
- apps/api/app/routers/posts.py
- apps/api/app/schemas/posts.py
- apps/api/app/models/content.py
</read_first>
<action>
1. 在 `PostRepository` 实现 `find_by_id_with_relations(post_id: UUID) -> Optional[Post]` 方法，预加载 author、cover_asset、assets、tags 关系

2. 在 `PostService` 实现 `get_post(post_id: UUID) -> Optional[PostResponse]` 方法，转换为 `PostResponse` schema（含 is_liked、is_favorited 字段）

3. 在 `apps/api/app/routers/posts.py` 实现 `get_post` 端点：
   - 返回 `PostResponse`
   - 404 时抛出 HTTPException
   - 检查作品 status=published，否则 404

4. 实现 `apps/api/app/routers/users.py` 的 `get_user_by_handle(handle: str) -> UserResponse` 端点
</action>
<acceptance_criteria>
- grep -r "def get_post" apps/api/app/ -> 找到 get_post 函数定义
- curl 测试: GET /api/v1/posts/{valid_uuid} 返回 200 和 PostResponse 结构
- curl 测试: GET /api/v1/posts/{invalid_uuid} 返回 404
</acceptance_criteria>
</task>

<task id="1-3-W2-03">
<name>创建前端 API 客户端 (packages/sdk/src/posts.ts)</name>
<read_first>
- apps/web/src/lib/data/mock.ts
- apps/api/app/schemas/posts.py
</read_first>
<action>
1. 创建 `packages/sdk/src/posts.ts`：
   - `getPosts(params: PostsQuery): Promise<PaginatedResponse<PostListResponse>>`
   - `getPost(postId: string): Promise<PostResponse>`
   - 类型定义匹配 `PostListResponse`、`PostResponse`、`PostsQuery`

2. 创建 `packages/sdk/src/users.ts`：
   - `getUserByHandle(handle: string): Promise<UserResponse>`

3. 创建 `packages/sdk/src/tags.ts`：
   - `getTags(): Promise<TagResponse[]>`

4. 导出所有 API 方法 from `packages/sdk/src/index.ts`
</action>
<acceptance_criteria>
- grep -r "export.*getPosts" packages/sdk/src/ -> 找到导出
- grep -r "PostListResponse" packages/sdk/src/ -> 类型已定义
- tsc --noEmit packages/sdk/src/posts.ts -> 无类型错误
</acceptance_criteria>
</task>

<task id="1-3-W2-04">
<name>创建首页瀑布流页面 apps/web/src/app/(feed)/page.tsx</name>
<read_first>
- apps/web/src/app/page.tsx
- apps/web/src/components/domain/WorkCard.tsx
- apps/web/src/lib/data/mock.ts
</read_first>
<action>
1. 创建 `(feed)` 路由组目录 `apps/web/src/app/(feed)/`

2. 创建 `apps/web/src/app/(feed)/layout.tsx`：
   - 使用现有 `Layout` 组件包装
   - 定义 feed 相关的 metadata

3. 创建 `apps/web/src/app/(feed)/page.tsx`（Server Component）：
   - 调用 `getPosts({ limit: 20, sort: 'latest' })` 获取初始数据
   - 渲染 `<FeedClient initialPosts={posts} />`

4. 创建 `apps/web/src/components/feed/FeedClient.tsx`（Client Component）：
   - 使用 `@tanstack/react-query` 的 `useInfiniteQuery`
   - 实现 Masonry 布局（CSS columns 或 react-masonry-css）
   - 实现无限滚动（IntersectionObserver 触发 `fetchNextPage`）
   - 分类筛选栏：全部/插画/漫画/CG/概念画
   - 排序切换：最新/热门/趋势
   - 每个作品渲染 `<WorkCard />`

5. 创建 `apps/web/src/components/feed/CategoryFilter.tsx`：
   - 横向滚动的分类筛选栏
   - 分类选项: 全部(All)、插画(Illustration)、漫画(Manga)、数字绘画(Digital Painting)、概念艺术(Concept Art)、动漫(Anime)
   - 高亮当前选中分类

6. 创建 `apps/web/src/components/feed/SortTabs.tsx`：
   - 三个排序选项: 最新(Latest)、热门(Popular)、趋势(Trending)
   - 切换时重置 infinite query

7. 更新根 `page.tsx` 为重定向到 `/(feed)`:
   - `export { default } from './(feed)/page'`
</action>
<acceptance_criteria>
- grep -r "Masonry" apps/web/src/components/feed/ -> 找到 Masonry 实现
- grep -r "useInfiniteQuery" apps/web/src/components/feed/ -> 找到无限滚动实现
- grep -r "CategoryFilter" apps/web/src/components/feed/ -> 找到分类筛选
- 访问 http://localhost:3000/ 显示瀑布流布局
- 滚动到底部加载更多作品
</acceptance_criteria>
</task>

<task id="1-3-W2-05">
<name>创建作品详情页 apps/web/src/app/post/[postId]/page.tsx</name>
<read_first>
- apps/web/src/components/domain/WorkCard.tsx
- apps/web/src/components/domain/CommentSection.tsx
- apps/api/app/schemas/posts.py
</read_first>
<action>
1. 创建 `apps/web/src/app/post/[postId]/page.tsx`（Server Component）：
   - `generateMetadata` 函数生成 SEO metadata（title、description、openGraph.images）
   - 调用 `getPost(params.postId)` 获取作品数据
   - 404 处理（作品不存在或非 published）

2. 创建 `apps/web/src/app/post/[postId]/page.tsx` 页面结构：
   - 作品展示区：左侧大图、右侧信息（标题/作者/标签/统计）
   - 评论区：`CommentSection` 组件
   - 点赞/收藏按钮（Client Component）
   - 分享按钮

3. 创建 `apps/web/src/components/post/PostActions.tsx`（Client Component）：
   - 点赞按钮：POST /api/v1/posts/{postId}/like
   - 收藏按钮：POST /api/v1/posts/{postId}/favorite
   - 本地状态乐观更新

4. 创建 `apps/web/src/components/post/PostGallery.tsx`（Client Component）：
   - 多图时显示缩略图导航
   - 点击放大查看

5. 更新 `WorkCard` 的链接从 `/work/` 改为 `/post/`
</action>
<acceptance_criteria>
- grep -r "generateMetadata" apps/web/src/app/post/ -> 找到 SEO 实现
- grep -r "PostActions" apps/web/src/components/post/ -> 找到互动按钮
- 访问 http://localhost:3000/post/{valid_id} 显示作品详情
- 访问 http://localhost:3000/post/{invalid_id} 显示 404
</acceptance_criteria>
</task>

<task id="1-3-W2-06">
<name>增强创作者主页 apps/web/src/app/creator/[handle]/page.tsx</name>
<read_first>
- apps/web/src/app/creator/[handle]/page.tsx
- apps/web/src/components/domain/WorkCard.tsx
</read_first>
<action>
1. 更新 `apps/web/src/app/creator/[handle]/page.tsx`：
   - 调用 `getUserByHandle(params.handle)` 获取创作者信息
   - 调用 `getPosts({ author_id: user.id, limit: 20 })` 获取作品列表
   - 替换 mock 数据为 API 数据

2. 创建 `apps/web/src/app/creator/[handle]/page.tsx` 中的关注功能：
   - 关注按钮：POST /api/v1/users/{handle}/follow
   - 本地状态乐观更新（is_following、follower_count）

3. 创建 `apps/web/src/components/creator/CreatorWorksGrid.tsx`（Client Component）：
   - 复用 Masonry 布局
   - 无限滚动加载更多作品
   - 空状态处理

4. 将 `WorkCard` 的作者链接从 `/profile/` 改为 `/creator/`
</action>
<acceptance_criteria>
- grep -r "getUserByHandle" apps/web/src/app/creator/ -> 找到 API 调用
- grep -r "follow" apps/web/src/app/creator/ -> 找到关注功能
- 访问 http://localhost:3000/creator/figmake-creator 显示创作者信息和作品
- 点击关注按钮状态正确切换
</acceptance_criteria>
</task>

<task id="1-3-W2-07">
<name>创建标签页 apps/web/src/app/(feed)/tags/[slug]/page.tsx</name>
<read_first>
- apps/web/src/app/(feed)/page.tsx
- apps/api/app/routers/posts.py
</read_first>
<action>
1. 创建 `apps/web/src/app/(feed)/tags/[slug]/page.tsx`（Server Component）：
   - 调用 `getTags()` 获取所有标签
   - 调用 `getPosts({ tag: params.slug, limit: 20 })` 获取该标签作品
   - generateMetadata 生成标签页 SEO

2. 复用 `(feed)/page.tsx` 的 `FeedClient` 组件，但传入 tag 筛选参数

3. 显示当前标签名称和作品数量

4. 在标签列表页添加标签云或下拉选择器
</action>
<acceptance_criteria>
- grep -r "tags" apps/web/src/app/\\(feed\\)/ -> 找到标签页实现
- 访问 http://localhost:3000/tags/anime 显示标签为 anime 的作品
- URL 参数变化时筛选结果正确更新
</acceptance_criteria>
</task>

<task id="1-3-W2-08">
<name>创建关注流/热门流页 apps/web/src/app/(feed)/following/page.tsx 和 trending/page.tsx</name>
<read_first>
- apps/web/src/app/(feed)/page.tsx
</read_first>
<action>
1. 创建 `apps/web/src/app/(feed)/following/page.tsx`（Server Component）：
   - 调用 `getPosts({ following: true, limit: 20 })` 获取关注用户作品
   - 未登录用户重定向到登录页
   - 复用 FeedClient 组件

2. 创建 `apps/web/src/app/(feed)/trending/page.tsx`（Server Component）：
   - 调用 `getPosts({ sort: 'trending', limit: 20 })` 获取热门作品
   - 复用 FeedClient 组件

3. 创建 `apps/web/src/components/feed/FeedHeader.tsx`：
   - 显示当前 feed 类型标题（最新动态/关注的人/热门作品）
</action>
<acceptance_criteria>
- grep -r "following" apps/web/src/app/\\(feed\\)/ -> 找到关注流页
- grep -r "trending" apps/web/src/app/\\(feed\\)/ -> 找到热门流页
- 访问 http://localhost:3000/following 显示关注流（未登录跳转）
- 访问 http://localhost:3000/trending 显示热门作品
</acceptance_criteria>
</task>

<task id="1-3-W2-09">
<name>创建搜索页 apps/web/src/app/search/page.tsx</name>
<read_first>
- apps/web/src/app/(feed)/page.tsx
- apps/web/src/components/domain/WorkCard.tsx
</read_first>
<action>
1. 创建 `apps/web/src/app/search/page.tsx`（Client Component）：
   - 搜索输入框（防抖 300ms）
   - 调用 `getPosts({ search: query, limit: 20 })` 搜索作品
   - 显示搜索结果数量

2. 创建 `apps/web/src/components/search/SearchResults.tsx`：
   - Masonry 布局显示搜索结果
   - 无限滚动加载更多
   - 空结果状态处理

3. 创建 `apps/web/src/components/search/SearchInput.tsx`：
   - 大尺寸搜索框，Editorial 风格
   - 支持回车搜索和点击搜索图标
   - 清空按钮

4. 后端 `apps/api/app/routers/posts.py` 的 `list_posts` 支持 `search` 参数：
   - 使用 `ilike` 模糊匹配 title 和 excerpt
</action>
<acceptance_criteria>
- grep -r "search" apps/web/src/app/search/ -> 找到搜索页实现
- grep -r "SearchInput" apps/web/src/components/search/ -> 找到搜索组件
- 访问 http://localhost:3000/search?q=neon 显示包含 neon 的作品
- 输入框防抖正确工作
</acceptance_criteria>
</task>

<task id="1-3-W2-10">
<name>集成测试与验收</name>
<read_first>
- apps/web/src/app/(feed)/page.tsx
- apps/api/app/routers/posts.py
</read_first>
<action>
1. 验证所有页面在开发服务器上正常运行（无 500 错误）

2. 验证首页瀑布流：
   - 初始加载 20 个作品
   - 滚动到底部加载更多
   - 分类筛选正确
   - 排序切换正确

3. 验证作品详情页：
   - 图片正确显示
   - 点赞/收藏状态切换
   - 评论区显示

4. 验证创作者主页：
   - 作品列表正确加载
   - 关注按钮功能正常

5. 验证标签页：
   - 标签筛选正确
   - URL 参数同步

6. 验证搜索页：
   - 搜索结果正确
   - 防抖生效

7. 运行 `pnpm build` 确保构建通过
</action>
<acceptance_criteria>
- pnpm build -> 构建成功，无错误
- curl GET /api/v1/posts?limit=2 -> 返回 items 数组
- curl GET /api/v1/posts?tag=anime -> 返回 tag=anime 的作品
- curl GET /api/v1/posts?sort=trending -> 返回按热门排序的作品
- curl GET /api/v1/posts?search=neon -> 返回包含 neon 的作品
- 前端页面无 console error
</acceptance_criteria>
</task>

</tasks>
