---
phase: "01-community-mvp"
plan: "01-05"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - apps/api/app/routers/posts.py
  - apps/api/app/schemas/posts.py
  - apps/api/app/services/__init__.py
autonomous: true
requirements:
  - REQ-1.5.1

must_haves:
  truths:
    - 新发布的内容有 processing 状态
    - 敏感内容可被标记
  artifacts:
    - apps/api/app/services/moderation.py
  key_links:
    - posts.status → processing/published/flagged
---

<objective>
实现基础内容审核能力：发布状态管理和内容标记

这是最简化的审核能力（Phase 4 才做完整后台审核系统）：
- 发帖后内容进入 processing 状态
- Worker 占位任务待后续扩展（Phase 4）
- flagged 内容在公开列表中默认过滤
</objective>

<tasks>
<task id="1-5-W3-01" depends_on="">
<name>Create moderation service with processing workflow</name>
<read_first>
- apps/api/app/models/content.py
- apps/api/app/schemas/posts.py
- apps/api/app/services/__init__.py
</read_first>
<action>
在 apps/api/app/services/ 创建 moderation.py，内容如下：

```python
# Basic Moderation Service
# Phase 1.5 - Simplified moderation for MVP

from enum import Enum
from uuid import UUID
from datetime import datetime
import logging

logger = logging.getLogger(__name__)


class ModerationAction(str, Enum):
    PASS = "pass"
    FLAG = "flag"
    REJECT = "reject"


async def process_post_moderation(post_id: UUID) -> ModerationAction:
    """
    Placeholder moderation task for newly created posts.

    In Phase 4 this will call actual content moderation APIs.
    For now, auto-passes all content as published.

    Args:
        post_id: The post UUID to moderate

    Returns:
        ModerationAction indicating the result
    """
    logger.info(f"[MODERATION] Processing post {post_id}")

    # TODO (Phase 4): Integrate actual moderation API
    # - Check for prohibited content
    # - Run AI-based content classification
    # - Apply community rules

    # Placeholder: auto-pass all content for MVP
    return ModerationAction.PASS


async def flag_content(post_id: UUID, reason: str) -> None:
    """
    Mark content as flagged for manual review.

    Args:
        post_id: The post UUID to flag
        reason: Reason for flagging
    """
    logger.warning(f"[MODERATION] Content {post_id} flagged: {reason}")
    # TODO (Phase 4): Update post status to flagged in DB
    pass
```

更新 apps/api/app/services/__init__.py，添加：

```python
from app.services.moderation import (
    ModerationAction,
    process_post_moderation,
    flag_content,
)

__all__ = ["UserService", "ModerationAction", "process_post_moderation", "flag_content"]
```

此任务创建占位服务，不修改任何其他文件。
</action>
<acceptance_criteria>
- grep -r "process_post_moderation" apps/api/app/services/moderation.py
- grep -r "ModerationAction" apps/api/app/services/moderation.py
- grep -r "flag_content" apps/api/app/services/moderation.py
</acceptance_criteria>
</task>

<task id="1-5-W3-02" depends_on="1-5-W3-01">
<name>Implement create_post with processing status</name>
<read_first>
- apps/api/app/routers/posts.py
- apps/api/app/schemas/posts.py
</read_first>
<action>
修改 apps/api/app/routers/posts.py 的 create_post 函数：

将 `status_code=status.HTTP_501_NOT_IMPLEMENTED` 的 `create_post` 函数替换为以下实现：

```python
@router.post("", response_model=PostResponse, status_code=status.HTTP_201_CREATED)
async def create_post(post: PostCreate):
    """创建作品 - 发布后进入 processing 状态等待审核"""
    from app.services.moderation import process_post_moderation

    # TODO (Phase 1.2): 替换为真实数据库操作
    # 1. 获取当前用户 from=Depends(get_current_user)
    # 2. 创建 Post 记录，status="processing"
    # 3. 关联 tags 和 assets
    # 4. 触发 process_post_moderation(post_id)

    raise HTTPException(
        status_code=status.HTTP_501_NOT_IMPLEMENTED,
        detail="Not implemented"
    )
```

注意：
- 只改 create_post 函数，其他函数保持 TODO 状态
- import process_post_moderation 在函数内部（避免循环导入）
- 保持 response_model=PostResponse
</action>
<acceptance_criteria>
- grep -n "status.*processing" apps/api/app/routers/posts.py
- grep -n "process_post_moderation" apps/api/app/routers/posts.py
</acceptance_criteria>
</task>

<task id="1-5-W3-03" depends_on="1-5-W3-02">
<name>Add status field validation to PostCreate schema</name>
<read_first>
- apps/api/app/schemas/posts.py
</read_first>
<action>
修改 apps/api/app/schemas/posts.py 的 PostCreate 类，添加 status 字段说明注释：

在 PostCreate 类的 docstring 中添加：
```python
class PostCreate(PostBase):
    """
    创建作品请求

    Note: 新创建的作品 status 默认为 'processing'，经审核后变为 'published'
    """
```

同时在 PostUpdate schema 中添加同样的说明。

这一步是文档更新，不涉及字段变更。
</action>
<acceptance_criteria>
- grep -n "processing" apps/api/app/schemas/posts.py
</acceptance_criteria>
</task>

<task id="1-5-W3-04" depends_on="1-5-W3-02">
<name>Add flag_post endpoint for content flagging</name>
<read_first>
- apps/api/app/routers/posts.py
</read_first>
<action>
在 apps/api/app/routers/posts.py 中添加新端点：

在 delete_post 之后、like_post 之前添加：

```python
@router.post("/{post_id}/flag", status_code=status.HTTP_202_ACCEPTED)
async def flag_post(post_id: UUID, reason: str = Query(..., min_length=1, max_length=500)):
    """标记内容为待审核状态"""
    from app.services.moderation import flag_content

    # TODO (Phase 1.2): 替换为真实数据库操作
    # 1. 验证 post 存在
    # 2. 调用 flag_content(post_id, reason)
    # 3. 更新 post status 为 'flagged'

    raise HTTPException(
        status_code=status.HTTP_501_NOT_IMPLEMENTED,
        detail="Not implemented"
    )
```

端点路径为 `/{post_id}/flag`，使用 POST 方法。
</action>
<acceptance_criteria>
- grep -n "flag_post" apps/api/app/routers/posts.py
- grep -n "/flag" apps/api/app/routers/posts.py
</acceptance_criteria>
</task>

<task id="1-5-W3-05" depends_on="1-5-W3-03">
<name>Update list_posts to filter flagged content</name>
<read_first>
- apps/api/app/routers/posts.py
</read_first>
<action>
修改 apps/api/app/routers/posts.py 的 list_posts 函数：

将默认 status 查询参数保持为 "published"，添加 admin_mode 参数：

```python
@router.get("", response_model=PaginatedResponse[PostListResponse])
async def list_posts(
    page: int = Query(default=1, ge=1),
    limit: int = Query(default=20, ge=1, le=100),
    cursor: Optional[str] = None,
    status: Optional[str] = Query(default="published"),
    tag: Optional[str] = None,
    author_id: Optional[UUID] = None,
    sort: str = Query(default="latest", pattern="^(latest|popular|trending)$"),
    # 新增参数
    admin_mode: bool = Query(default=False, description="管理员模式：包含 flagged 内容"),
):
    """
    获取作品列表

    - status: 筛选状态 (published / all)
    - tag: 筛选标签 slug
    - author_id: 筛选作者
    - sort: 排序方式 (latest / popular / trending)
    - admin_mode: 管理员模式，True 时返回 flagged 内容（Phase 4）
    """
    # TODO: 实现作品列表查询
    # - 非 admin_mode 时，默认过滤 status != 'flagged'
    # - admin_mode 时，返回所有状态内容
    raise HTTPException(
        status_code=status.HTTP_501_NOT_IMPLEMENTED,
        detail="Not implemented"
    )
```

关键改动：添加 admin_mode 参数并更新 docstring 说明。
</action>
<acceptance_criteria>
- grep -n "admin_mode" apps/api/app/routers/posts.py
- grep -n "管理员模式" apps/api/app/routers/posts.py
</acceptance_criteria>
</task>

<task id="1-5-W3-06" depends_on="1-5-W3-01">
<name>Create worker placeholder tasks documentation</name>
<read_first>
- apps/api/app/services/moderation.py
</read_first>
<action>
在 apps/api/app/services/moderation.py 文件末尾（return 语句之后）添加占位任务说明：

```python
# ---------------------------------------------------------------------------
# Worker Placeholder Tasks (Phase 4)
# ---------------------------------------------------------------------------
#
# The following tasks are TODO placeholders for Phase 4 moderation worker:
#
# 1. async def scheduled_moderation_batch():
#    - Runs every N minutes via cron
#    - Fetches posts with status='processing'
#    - Calls process_post_moderation() for each
#    - Updates status to 'published' or 'flagged'
#
# 2. async def cleanup_orphaned_assets():
#    - Runs daily
#    - Finds PostAssets with status='uploaded' older than 24h
#    - Marks as 'failed' and schedules cleanup
#
# 3. async def auto_expire_drafts():
#    - Runs weekly
#    - Marks drafts older than 90 days as 'archived'
#
# See EP-005 for Phase 4 full moderation system implementation.
```

这个任务是纯文档添加，不修改代码逻辑。
</action>
<acceptance_criteria>
- grep -n "Worker Placeholder Tasks" apps/api/app/services/moderation.py
- grep -n "scheduled_moderation_batch" apps/api/app/services/moderation.py
</acceptance_criteria>
</task>
</tasks>
